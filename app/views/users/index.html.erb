<h1>All users</h1>

<%= will_paginate %>
<ul class="users">
  <% #@users.each do |user| #dont need this loop, render @users loops automatically thru each user
	%>
    <%= render @users %>
  <% # end
	 %>
</ul>

<%= will_paginate %>
